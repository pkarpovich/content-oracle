export const BaseURL = import.meta.env.VITE_API_BASE_URL || window.location.origin;
// TODO: remove this before production
export const HubBaseURL = import.meta.env.VITE_HUB_BASE_URL || window.location.origin;
